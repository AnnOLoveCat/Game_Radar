services:
  api:
    build: .
    container_name: game-radar-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////app/data/game_radar.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped